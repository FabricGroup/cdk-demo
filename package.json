{
  "name": "cdk",
  "version": "0.1.0",
  "bin": {
    "cdk": "bin/cdk.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "yarn build && jest",
    "precdk": "yarn build",
    "cdk": "cdk",
    "precdk-synth": "rm -rf cdk.out",
    "cdk-synth": "yarn cdk synth",
    "cdk-deploy": "yarn cdk synth && cdk deploy"
  },
  "devDependencies": {
    "@aws-cdk/assert": "^1.12.0",
    "@types/jest": "^24.0.18",
    "jest": "^24.9.0",
    "ts-jest": "^24.0.2",
    "aws-cdk": "^1.12.0",
    "ts-node": "^8.1.0",
    "typescript": "~3.6.2"
  },
  "dependencies": {
    "@aws-cdk/core": "^1.12.0",
    "source-map-support": "^0.5.9"
  }
}
